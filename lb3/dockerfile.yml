version: '3.1'

services:
  # Database
  db:
    image: mysql:8.0.24
    volumes:
      - db_data:/var/lib/mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: Pa$$w0rd
      MYSQL_DATABASE: phpbase
      MYSQL_USER: raphael
      MYSQL_PASSWORD: Pa$$w0rd
